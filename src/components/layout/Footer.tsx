export function Footer() {
  return (
    <footer className="app-footer">
      <span>Production console for deterministic assignments, exposure tracking, and variant metrics.</span>
      <span>No raw user identifiers are persisted in browser storage.</span>
    </footer>
  );
}
